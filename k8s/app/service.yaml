# k8s/app/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: spring-service
  namespace: reactive-app
  labels:
    app: spring-app
spec:
  type: ClusterIP
  selector:
    app: spring-app
  ports:
    - port: 80
      targetPort: 8080